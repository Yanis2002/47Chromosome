# Автоматическая генерация контента

## Описание

Скрипт `generate-content.js` автоматически создает JSON файлы и обновляет код на основе файлов в папках.

## Использование

Просто запустите скрипт:

```bash
node generate-content.js
```

## Что делает скрипт

1. **Музыка** (`docs/music/`)
   - Сканирует все аудио файлы (.mp3, .wav, .ogg, .flac, .m4a)
   - Автоматически обновляет функцию `loadLocalMusic()` в `script.js`
   - Генерирует названия из имен файлов

2. **Фото** (`docs/photo/`)
   - Сканирует все изображения (.jpg, .jpeg, .png, .webp, .gif, .svg)
   - Автоматически обновляет файл `photo/list.json`
   - Генерирует описания из имен файлов

3. **Видео** (`docs/video/`)
   - Сканирует все видео файлы (.mp4, .webm, .ogg, .mov, .avi)
   - Генерирует код для функции `loadLocalVideos()` в `script.js`

## Пример работы

```bash
$ node generate-content.js

🚀 Начинаю генерацию контента...

🎵 Генерация списка музыки...
✅ Обновлен script.js: добавлено 66 треков
📸 Генерация списка фото...
✅ Обновлен photo/list.json: добавлено 9 фото
🎬 Генерация списка локальных видео...
✅ Обновлен script.js: добавлено 0 видео

✨ Готово!
   🎵 Музыка: 66 треков
   📸 Фото: 9 изображений
   🎬 Видео: 0 файлов
```

## Важно

- Скрипт **НЕ** попадает на сайт (добавлен в `.gitignore`)
- Запускайте скрипт **локально** после добавления новых файлов
- После запуска скрипта закоммитьте изменения в Git

## Когда использовать

Запускайте скрипт когда:
- Добавили новые музыкальные файлы в `docs/music/`
- Добавили новые фото в `docs/photo/`
- Добавили новые видео в `docs/video/`

Скрипт автоматически обновит все необходимые файлы!
